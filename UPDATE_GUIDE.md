# 갱신 작업 지침 (원문 변경분 반영)

기본 규칙은 `TRANSLATION_GUIDE.md`와 같다 (용어 통일표, 섹션 제목표, 출처 태그·코드·주소·링크 원문 그대로, 평서체, H1 다음 줄에 `<!-- source: wiki/... -->`).

## 두 가지 작업 종류

### (A) 패치: 기존 번역본에 diff 반영
- 입력: 원문 diff 파일(`old/` → `new/`), 기존 한글 번역본, 원문 새 버전 전체.
- 방법: diff의 `-` 줄에 해당하는 한글 문장을 찾아 `+` 줄의 내용으로 고친다. 추가된 문단·표 행·항목은 새로 번역해 같은 자리에 넣는다. 삭제된 것은 지운다.
- diff에 없는 부분은 절대 손대지 않는다.
- 끝나면 새 원문과 번역본의 헤딩 수, 표 행 수, `[S:`/`[E:`/`[O:`/`[H:`/`[P:` 태그 수가 같은지 세어 확인한다.

### (B) 전체 재번역
- 원문 새 버전 전체를 처음부터 번역해 기존 번역본을 덮어쓴다. 기존 번역본은 용어·문체 참고용으로 먼저 읽는다.

## 파일 쓰기 주의
- Bash heredoc(`cat <<'EOF'`)은 이 환경에서 깨진다. 긴 파일은 Write 도구로 부분 파일(part1.md, part2.md…)을 스크래치 디렉터리에 쓰고 `cat part1.md part2.md > 대상` 으로 합친다.
- 완료 보고에 원문/번역본 줄 수와 위 구조 카운트를 적는다.
